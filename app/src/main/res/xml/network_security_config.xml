<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!--
        HTTPS is enforced (no cleartext). Certificate pinning is not configured
        for the MosaicOS repo (appstore.mosaicos.io) yet
    -->
    <base-config cleartextTrafficPermitted="false"/>
</network-security-config>
